# main_threshold 기본값 분석: 56% vs 60%

## 1. 현재 코드 상태

### 함수 파라미터 기본값: **60%**
사용 위치:
- `simulate_single_threshold(main_threshold=60)` - 시뮬레이션 핵심 함수
- `random_search_multi_dimensional(main_threshold=60)` - 다차원 최적화
- `batch_simulate_threshold_range(main_threshold=60)` - 배치 시뮬레이션
- `hybrid_search_optimal_threshold(main_threshold=60)` - 하이브리드 탐색

### UI 기본값: **56%**
사용 위치:
- `optimal_threshold_finder_app.py`: UI 입력 기본값 56%
- `live_game_app.py`: 기본값 56%
- `betting_strategy_simulator.py`: 기본값 56.0%

### 다른 앱: **60%**
- `interactive_multi_step_validation_app.py`: 기본값 60%

## 2. 사용 패턴 분석

### 함수 레벨 (코어 로직)
- **60%가 일관되게 사용됨**
- 함수 파라미터 기본값이 모두 60%로 통일
- 이는 "코드의 의도"를 나타냄

### UI 레벨 (사용자 인터페이스)
- **56%가 더 많이 사용됨**
- `optimal_threshold_finder_app.py`, `live_game_app.py` 모두 56%
- 이는 "실제 사용 경험"을 반영한 것으로 보임

### Session State 기본값
- 56% 사용 (`st.session_state.get('simulation_main_threshold', 56)`)
- 사용자가 설정하지 않을 때 56% 사용

## 3. 합리적 판단 기준

### 기준 1: 코드 일관성
- 함수 파라미터 기본값이 60%로 통일되어 있음
- 이를 56%로 변경하면 여러 함수 수정 필요
- ⚠️ 일관성 유지를 위해 60% 유지 권장

### 기준 2: 실제 사용 경험
- UI에서 56%를 기본값으로 사용하는 것은 실제 테스트 결과를 반영한 것일 수 있음
- 56%가 더 적합한 성능을 보였을 가능성
- ✅ 사용자 편의를 위해 56% 유지 권장

### 기준 3: 역할과 목적
- `main_threshold`는 "예측 시도 기준"
- 60%는 더 보수적 (높은 신뢰도만 예측)
- 56%는 더 공격적 (더 많은 예측 시도)
- 56%가 강제 예측이 더 자주 발생하여 스킵 임계값 테스트에 더 적합할 수 있음

### 기준 4: 시뮬레이션 목적
- 시뮬레이션의 목적은 `confidence_skip_threshold` 최적화
- `main_threshold`는 고정 파라미터로 사용
- 56%면 강제 예측이 더 자주 발생하여 스킵 임계값의 효과를 더 잘 테스트할 수 있음
- ✅ 56%가 시뮬레이션 목적에 더 적합

## 4. 권장 사항

### 선택: **56%로 통일** ✅

**이유:**
1. **실제 사용 경험 반영**: UI에서 이미 56% 사용 중
2. **시뮬레이션 목적 적합**: 강제 예측 빈도가 높아 스킵 임계값 테스트에 유리
3. **사용자 일관성**: 라이브 게임과 시뮬레이션에서 동일한 기본값 사용

**수정 사항:**
- 함수 파라미터 기본값을 60% → 56%로 변경
- 코드 전반의 일관성 확보

### 대안: **60%로 통일** (현재 상태 유지)

**이유:**
1. 코드 일관성 유지
2. 더 보수적인 예측 (높은 신뢰도만)
3. 함수 레벨에서 이미 통일됨

**단점:**
- UI와 함수 파라미터의 불일치 지속
- 사용자 혼란 가능성

## 5. 결론

**권장: 56%로 통일**

실제 사용 경험과 시뮬레이션 목적을 고려할 때, **56%가 더 합리적**입니다.

<｜tool▁calls▁begin｜><｜tool▁call▁begin｜>
read_file